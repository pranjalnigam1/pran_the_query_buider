# React Query Builder

A flexible React component to build query rules with grouping, drag-and-drop, and export to JSON.

## Usage

```tsx
import { QueryBuilder } from "react-query-builder";

<QueryBuilder fields={[{ label: "Name", value: "name" }]} />
```